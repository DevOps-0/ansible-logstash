---
- name: Update Package Cache
  apt:
    update_cache: yes

- name: Reload Service | monit
  command: monit reload

- name: Reload Service | logstash
  monit:
    state: restarted
    name: logstash
  tags:
    - monit
    - disruptive
