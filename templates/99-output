# {{ ansible_managed }}

output {
  stdout { codec => rubydebug }
  elasticsearch {
    host => "{{ logstash_elasticsearch_host }}"
    cluster => "elasticsearch_cluster_production"
    #user => "digital"
    #password => "notwebchannel"
    index => "logstash-%{project}-%{environment_tier}-%{+YYYY.MM.dd}"
  }
}
