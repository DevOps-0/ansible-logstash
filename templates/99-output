# {{ ansible_managed }}

output {
  elasticsearch {
    hosts => ["{{ logstash_elasticsearch_host }}"]
    index => "logstash-%{project}-%{environment_tier}-%{+YYYY.MM.dd}"
    workers => 8
    flush_size => 1000
    idle_flush_time => 1
  }
}
